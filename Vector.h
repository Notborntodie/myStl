template <typename T>
class Vector
{
private:

    /* data */
public:

    Vector(
        /* args */);
    ~Vector();
};

template<typename T>
Vector<T>::Vector(/* args */)
{template


}

template<typename T>
Vector<T>::~Vector()
{
}
